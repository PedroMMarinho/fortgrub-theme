from helpers.utils import load_config, save_config
from modifiers.insert_emoticon import change_emoticon

def run():
    print("Starting Theme Generation ...")

    # Load main config
    config = load_config()

    # Generate final images

    print("Theme generation completed successfully!")